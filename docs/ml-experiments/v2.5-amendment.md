# Predictive Generalization V2.5 Fault 解释性计算修订

状态：`LOCKED AFTER DEVELOPMENT SELECTION AND BEFORE FROZEN TEST ACCESS`

## 原因

F0-F4 的 50 个 preregistered candidates 已全部完成，Development winner 已按锁定规则确定为 F4 RandomForest。selected-only explainability 首次采用每折最多 1,000 行、93 个特征、3 次 permutation repeat；该计算在 120 秒命令上限内未完成并被终止。summary 在 explainability 完成后才写入，因此中断没有覆盖候选 JSON、Development metrics 或原 summary，也没有访问 Frozen Test。

## 修改

Fault selected-only permutation importance 改为每个 held-out Development fold 中确定性等距选取最多 250 行。仍遍历全部 93 个特征、固定 `n_repeats=3`、固定 seed，并在五个固定 folds 上聚合。

以下内容不变：

- 所有候选预测、OOF/fold metrics 与 Development winner；
- F0-F4、模型、hyperparameters、threshold 与 selection order；
- macro recall `>= 0.70`、healthy recall `>= 0.60`；
- Stage 2 结果；
- Frozen Test 仍未访问。

Permutation importance 从未参与调参、选模或 promotion。本修订只控制选定候选的事后解释性成本。
